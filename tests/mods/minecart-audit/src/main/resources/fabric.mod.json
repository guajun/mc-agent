{
  "schemaVersion": 1,
  "id": "mc-agent-minecart-audit",
  "version": "0.1.0",
  "name": "MC Agent Minecart Audit",
  "description": "Independent, read-only server-side audit of machine inputs and transient minecart outputs. Test-side evidence only; the agent still has to build its own logger.",
  "environment": "*",
  "entrypoints": {
    "main": [
      "dev.mcagent.audit.AuditMod"
    ]
  },
  "mixins": [
    "mcaudit.mixins.json"
  ],
  "depends": {
    "fabricloader": ">=0.19.0",
    "minecraft": "*",
    "fabric-api": "*",
    "java": ">=25"
  }
}
