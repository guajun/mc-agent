实际运行游戏并研究，通过音符盒运行用户面前的机器，得到每个弹出的矿车中物品的有序列表。

探索要求：

- 即使通过内存状态和已有知识已能确信答案，也必须真实操作音符盒，并以动态记录验证。
- 不得修改机器或库存来制造结果。
- 允许正常研究、完整 NBT/顺序读取、脚本及自行编写、构建、安装 logger mod，不设能力沙箱。
- 允许先读取完整库存，再以实际弹出的矿车 UUID 关联输出；不得只凭推理直接给出答案。

本文件是冷启动 Agent 收到的任务输入。它不包含标准答案、先前成功脚本或解题轨迹。
