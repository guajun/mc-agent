# Build identity substituted by tools/build_mod.py (${version}); the two
# same-size iteration jars differ only in this value, which keeps the jar
# byte length identical while changing the SHA-256.
build=${version}
