{
  "schemaVersion": 1,
  "id": "rom20-agent-logger",
  "version": "${version}",
  "name": "ROM20 Agent Logger",
  "description": "Agent-owned logger: captures each ejected chest minecart with its ordered inventory.",
  "environment": "*",
  "entrypoints": {
    "main": [
      "dev.mcagent.romlog.RomLogMod"
    ]
  },
  "mixins": [
    "romlog.mixins.json"
  ],
  "depends": {
    "fabricloader": ">=0.19.0",
    "minecraft": "*",
    "fabric-api": "*",
    "java": ">=25"
  }
}
